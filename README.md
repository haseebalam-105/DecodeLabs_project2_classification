Project 2 — Data Classification Using AI (project2_classification.py)
Full supervised learning pipeline on the Iris benchmark: StandardScaler feature scaling → 80/20 shuffled train-test split → KNN (k=5) with fit → predict → output includes confusion matrix, F1 score, full classification report, and a bonus custom sample prediction. Achieved 100% accuracy on the test split. Requires scikit-learn.
